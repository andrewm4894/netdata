## Insights

Location for some Machine Learning and Statistical based features.

### metric_correlations

Example usage:

```
python metric_correlations.py \
  --baseline_after='-120' \
  --baseline_before='-60' \
  --highlight_after='-60' \
  --highlight_before='0' \
  --model='ks' \
  --n_lags='2'
```